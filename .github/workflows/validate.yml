name: Validate playbook

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install PyYAML
        run: python -m pip install --quiet pyyaml

      - name: Run playbook validation
        # ubuntu-latest ships pwsh, so scripts/validate.sh also runs the
        # launcher parity check here.
        run: bash scripts/validate.sh

  powershell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Parse install.ps1
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
            (Resolve-Path ./install/install.ps1).Path, [ref]$null, [ref]$errors) | Out-Null
          if ($errors -and $errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Host $_ }
            throw "install.ps1 has $($errors.Count) parse error(s)."
          }
          Write-Host "install.ps1 parses cleanly."

      - name: Full install through the PowerShell launcher
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $home2 = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          $env:CLAUDE_CONFIG_DIR = $home2
          ./install/install.ps1 -Full
          if ($LASTEXITCODE -ne 0) { throw "install.ps1 exited $LASTEXITCODE" }
          foreach ($p in @(
            'CLAUDE.md',
            'agents/local-orchestrator.md',
            'skills/senior-code-review/SKILL.md',
            'skills/subagent-orchestration/references/model-routing.md',
            'skills/reference-doc-routing/references/engineering-design.md',
            'skills/task-graph-orchestration/references/templates/task-graph.md'
          )) {
            $full = Join-Path $home2 $p
            if (-not (Test-Path -LiteralPath $full)) { throw "missing $full" }
          }
          if (Test-Path -LiteralPath (Join-Path $home2 'references')) { throw "installer created a top-level references directory" }
          Write-Host "install.ps1 full install succeeded."

      - name: Dry run through the PowerShell launcher creates nothing
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $home3 = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          $env:CLAUDE_CONFIG_DIR = $home3
          $out = ./install/install.ps1 -Full -DryRun
          if ($LASTEXITCODE -ne 0) { throw "install.ps1 -DryRun exited $LASTEXITCODE" }
          if (Test-Path -LiteralPath $home3) { throw "dry run created $home3" }
          if (-not ($out -match 'Dry run complete. No files were changed.')) { throw "dry run did not report that nothing changed" }
          Write-Host "install.ps1 dry run created nothing."

      - name: Fail loudly on a missing managed file
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'
          $clone = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          New-Item -ItemType Directory -Force -Path $clone | Out-Null
          foreach ($d in @('agents','skills','custom-instructions','install')) {
            Copy-Item -Recurse -Path $d -Destination (Join-Path $clone $d)
          }
          Remove-Item -Recurse -Force (Join-Path $clone 'skills/senior-code-review')
          $env:CLAUDE_CONFIG_DIR = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          & (Join-Path $clone 'install/install.ps1') -Full *> $null
          if ($LASTEXITCODE -eq 0) { throw "install.ps1 exited 0 despite a missing managed file." }
          Write-Host "install.ps1 correctly exits non-zero."
